from typing import Tuple

def compute_nash_equilibrium(payoff_matrix) -> Tuple[float, float]:
    """Helper for pure game theory solver if needed beyond basic ADT logic."""
    # Placeholder
    return (0.5, 0.5)
